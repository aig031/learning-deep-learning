데이터를 구분하여, 70%정도를 training set, 30%정도를 test set이라 가정한다.

training set -> model -> test set을 통하여, 예측값과 실제 값을 비교.

       original set
<--------------------------------> 
      training          | testing
  training   :Validation| testing

training -> 교과서
testing -> 시험
validation -> 모의고사

Online learning -> 새로운 data를 추가로 model에 학습시키는 것

MINIST Dataset -> 숫자를 구분 할 수 있도록 해주는 data set. (training & testing으로 나뉘어져 있다.)

Accuracy -> 예측 값 = 실제 값 (현재 95~99%)
